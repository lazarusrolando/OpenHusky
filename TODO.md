- [ ] Understand torch warning source (torch.compile + Enum constant registration)
- [ ] Update `test.py` to disable torch.compile by default and/or gate behind an env var to remove warnings
- [ ] Optional: suppress only the specific warning category in a targeted way if gating is not sufficient
- [ ] Run `python test.py` quick sanity check

